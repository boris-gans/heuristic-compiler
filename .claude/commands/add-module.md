# Add a module

Create a new module for [FEATURE].

Requirements:
- use a clear, typed public interface
- keep functions small and composable
- add error handling with useful logging context
- avoid unnecessary abstraction
- include a minimal unit test using a fake, fixture, or stub
- if relevant, show how the module is wired into the project
- update `.claude/context.md` if this changes architecture, capabilities, dependencies, or workflows

Output:
1. brief design summary
2. code changes
3. test
4. context updates if any
5. assumptions or follow-ups